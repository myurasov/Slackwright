# Python
__pycache__/
*.py[cod]
*.egg-info/
*.egg
.eggs/
dist/
build/

# Virtual envs
.venv/
venv/

# Test / lint / type-checker caches
.pytest_cache/
.ruff_cache/
.mypy_cache/
.coverage
htmlcov/

# Editor
.vscode/
.idea/
*.swp

# OS
.DS_Store
Thumbs.db

# Slackwright runtime artifacts (can contain auth tokens / cookies / scraped data)
slackwright-out/
.slackwright-state/

# Playwright browsers (large, machine-local)
.playwright-browsers/

**/__*

# Solaris ai-pack (embedded mode): keep the private memory layer + runtime MCP out of the repo.
# The rest of ai/ (engineer.agent.md, engineer.instructions.md, spec.md, manifest.json) + AGENTS.md +
# CLAUDE.md are the embedded pack and may be committed with the repo.
ai/memory/
.secrets.env
.mcp.json
.cursor/mcp.json
